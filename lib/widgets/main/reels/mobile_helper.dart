import 'dart:typed_data';

String? createVideoUrl(Uint8List videoBytes) {
  // Not needed for mobile, return null
  return null;
}

void revokeVideoUrl(String url) {
  // Not needed for mobile, do nothing
}
